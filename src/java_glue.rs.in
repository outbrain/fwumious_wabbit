
use crate::session::FWSession;
use crate::session::FWPort;

use crate::jni_c_header::*;

foreign_class!(class FWSession {
    self_type FWSession;
    constructor FWSession::new(cl_string: &str) -> FWSession;
});

foreign_class!(class FWPort {
    self_type FWPort;
    constructor FWPort::new(fws: &FWSession) -> FWPort;
    fn FWPort::learn(&mut self, fws: &mut FWSession, input_buffer: &str) -> f32;
    fn FWPort::predict(&mut self, fws: &mut FWSession, input_buffer: &str) -> f32;
});
